# Verana Trust Resolver — Environment Variables (spec.md §5.2)

# Polling interval in seconds
POLL_INTERVAL=5

# Max age for downloaded files (DID docs, VCs, VPs) before refresh
CACHE_TTL=86400

# Max age for trust evaluation results before re-evaluation
TRUST_TTL=3600

# Retry window (days) for failed dereferencing attempts
POLL_OBJECT_CACHING_RETRY_DAYS=7

# PostgreSQL connection string
DATABASE_URL=postgresql://localhost:5432/verana_resolver

# Redis connection string
REDIS_URL=redis://localhost:6379

# Instance role: leader | reader
INSTANCE_ROLE=leader

# Server port
PORT=3000

# Log level: debug | info | warn | error
LOG_LEVEL=info

# VPR allowlist config file path
VPR_ALLOWLIST_PATH=config/vpr-allowlist.json
